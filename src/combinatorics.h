#ifndef combinatorics_h
#define combinatorics_h

#include <array>
#include <vector>

unsigned long long comb(unsigned, unsigned);

std::vector<std::vector<unsigned> > combinationsWithReplacement(unsigned, unsigned);

#endif
